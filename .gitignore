/target
**/*.rs.bk

# Renders produced by the golden harness. The committed *references* live in
# golden/refs and are tracked; anything under out/ is a test artefact.
/golden/out

# Third-party licence manifest, regenerated by `cargo about generate`.
/THIRD-PARTY.md

# Sample RAW files are large and are not redistributable; keep them out of git.
*.arw
*.ARW
*.cr3
*.CR3
*.nef
*.NEF
*.raf
*.RAF
*.dng
*.DNG

# UI shell (Tauri) lands in P0 step 7.
node_modules/
dist/

.DS_Store
/probe*.xwd

# Tauri regenerates these from tauri.conf.json on every build.
crates/rawkit-shell/gen/schemas/

[package]
name = "rawkit-deliver"
description = "Catalog selection to files on disk: the layer that composes decode, render and encode. Shared by every front end, so an export means the same thing from the terminal and from the window."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
rawkit-editstate.workspace = true
rawkit-decode.workspace = true
rawkit-engine.workspace = true
rawkit-catalog.workspace = true
rawkit-export.workspace = true
anyhow.workspace = true
